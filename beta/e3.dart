class Exercise {
  bool? exercise() {
    // Deklarasikan sebuah variabel bernama 'count' dengan tipe data int dan berikan nilai 0

    // Increment nilai variabel 'count' sebanyak 3 kali

    // Tulis kode IF Statement untuk memeriksa apakah nilai variabel 'count' sama dengan 3
    // Jika sama, kembalikan true, jika tidak, kembalikan false
  }

  bool testCases() {
    if (exercise() != true) throw Exception(true);
    return true;
  }
}
