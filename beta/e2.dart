class Exercise {
  bool? exercise() {
    // Deklarasikan sebuah variabel bernama 'age' dengan tipe data int dan berikan nilai 25

    // Deklarasikan sebuah variabel bernama 'year' dengan tipe data int dan berikan nilai 2023

    // Tulis kode IF Statement untuk memeriksa apakah nilai variabel 'age' dikali nilai variabel 'year' lebih besar dari 50000
    // Jika lebih besar, kembalikan true, jika tidak, kembalikan false
  }

  bool testCases() {
    if (exercise() != true) throw Exception(true);
    return true;
  }
}
