class Exercise {
  bool? exercise() {
    // Deklarasikan sebuah variabel bernama 'name' dengan tipe data String dan berikan nilai "John"

    // Tulis kode IF Statement untuk memeriksa apakah nilai variabel 'name' sama dengan "John"
    // Jika sama, kembalikan true, jika tidak, kembalikan false
  }

  bool testCases() {
    if (exercise() != true) throw Exception(true);
    return true;
  }
}
